
public class Solution {
    public bool IsPalindrome(string s)
    {
        // the goal is to determin if a string is a valid palindrom after removing all non-alphanumeric characters and ignoring case
        //  and converting all characters to lower case
        // topics - String, Two Pointers

        // confirm edge cases
        // a string with only non-aplhanumeric characters like "!!" -> becomes empty -> it is a palindrome and return true

        // aim for O(n) time and O(1) extra space if possible

        //Approach
        // two pointers
        // one pointer at the start and one at the end
        // Move the pointers towwards the center


    
    }
}
